# Core MCP and AWS dependencies
fastmcp>=2.11.0
mcp>=1.12.0
boto3>=1.34.0
botocore>=1.34.0

# MCP client for server communication
mcp-client>=0.5.0
httpx>=0.25.0
websockets>=11.0.0

# Dashboard and visualization
streamlit>=1.28.0
pandas>=2.0.0
plotly>=5.17.0

# Diagram generation
diagrams>=0.23.0
graphviz>=0.20.0

# Async and utilities
pydantic>=2.0.0
python-dotenv>=1.0.0
aiofiles>=23.0.0

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0

# AWS Labs MCP servers (install via npm)
# npm install -g @awslabs/aws-dataprocessing-mcp-server
# npm install -g @awslabs/dynamodb-mcp-server
# npm install -g @awslabs/s3-tables-mcp-server
# npm install -g @awslabs/aws-diagram-mcp-server